"""Original-language cinema screenings in Berlin, from ov-berlin.info.

``ovb`` is the terminal listing, ``ovd-report`` the Telegram digest. Runtime
files (``.env``, ``config/``, ``.cache/``, ``run-history.json``, the collage)
live in the project root, the parent of this package.
"""
